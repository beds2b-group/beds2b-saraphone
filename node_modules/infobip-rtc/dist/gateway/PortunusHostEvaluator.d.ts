import { AccessToken } from "./AccessToken";
import { Logger } from "../log/Logger";
export declare class PortunusHostEvaluator {
    private logger;
    constructor(logger: Logger);
    getPortunusHosts(token: AccessToken): string[];
    generatePortunusHost(location: string): string;
    evaluate(token: AccessToken): Promise<string>;
    protected checkIfAvailable(portunusHost: string): Promise<string>;
}
