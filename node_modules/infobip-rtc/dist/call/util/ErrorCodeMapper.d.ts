import { ErrorCode } from "../event/ErrorCode";
declare class ErrorCodeMapper {
    static fromRuntimeError(error: unknown): ErrorCode;
    static fromBackendStatus(status: unknown): ErrorCode;
    private static normalizeStatus;
    private static isErrorLike;
    private static normalizeString;
    private static getFallbackDescription;
}
export default ErrorCodeMapper;
